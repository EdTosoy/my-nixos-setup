# Copy this file to secrets.nix and fill in your values.
#
#   cp secrets.nix.example secrets.nix
#
# secrets.nix is gitignored and will never be committed.
# If secrets.nix is missing, the build will succeed but no password will be set.



{ ... }:
{
  users.users.johncarlojose.initialPassword = "your-password-here";
}